{
  "name": "webumkm-root",
  "version": "1.0.0",
  "description": "Root package for WebUMKM client and server",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm --prefix client dev\" \"pnpm --prefix server start\"",
    "build": "pnpm --prefix client build",
    "start": "pnpm --prefix server start && pnpm --prefix client serve"
  },
  "keywords": [],
  "author": "liaranda",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "serve": "^14.2.4"
  }
}
